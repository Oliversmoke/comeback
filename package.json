{
  "name": "stakemind",
  "private": true,
  "description": "StakeMind — AI-Powered Accountability on Stellar Soroban",
  "scripts": {
    "dev": "concurrently -n server,client -c blue,green \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "dev:server": "npm run dev --prefix server",
    "dev:client": "npm run dev --prefix client",
    "start": "concurrently -n server,client -c blue,green \"npm start --prefix server\" \"npm start --prefix client\"",
    "install:all": "npm install --prefix server && npm install --prefix client",
    "contracts:build": "cd comeback-contract && cargo build --target wasm32-unknown-unknown --release",
    "contracts:test": "cd comeback-contract && cargo test",
    "sdk:build": "cd packages/sdk && npm run build",
    "indexer:start": "cd indexer && npm start"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
