{
  "name": "@stakemind/sdk",
  "version": "0.1.0",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "npm run build && node --test",
    "smoke:stake": "npm run build && node scripts/smoke-test-stake.mjs",
    "verify:client-env": "npm run build && node scripts/verify-client-env.mjs"
  },
  "dependencies": {
    "@stellar/stellar-sdk": "^13.0.0"
  },
  "devDependencies": {
    "typescript": "^5.8.2"
  }
}
